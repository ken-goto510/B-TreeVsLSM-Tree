spring:
  datasource:
    url: jdbc:mysql://localhost:3307/benchmark_db?useSSL=false&allowPublicKeyRetrieval=true&rewriteBatchedStatements=true
    username: benchmark
    password: benchmark
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      connection-timeout: 30000

  sql:
    init:
      mode: never

server:
  port: 8080

logging:
  level:
    com.example: INFO